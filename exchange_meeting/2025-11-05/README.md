# typst-flake-template

```sh
curl -L https://github.com/beleap/typst-flake-template/archive/refs/heads/master.zip -o repo.zip
unzip repo.zip -d .
mv typst-flake-template-master <name>
rm repo.zip
```
